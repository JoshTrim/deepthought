[tool.poetry]
name = "Deepthought"
version = "0.1.0"
description = ""
authors = ["Josh Trim <josh.trim@tuta.io>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
ffmpeg-python = "^0.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
